import styled from 'styled-components';
import { size } from '../../../styles/Theme';

export const DropdownWrapper = styled.div`
  display: flex;
  width: 95%;
  margin-right: ${size.SIZE_012};
  margin-top: ${size.SIZE_010};
  justify-content: flex-end;
`;
