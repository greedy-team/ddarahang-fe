import styled from 'styled-components';
import { size } from '../../../styles/Theme';

export const PaginationContainer = styled.header`
  width: 100vw;
  display: flex;
  margin-top: ${size.SIZE_014};
  margin-bottom: ${size.SIZE_014};
  align-items: center;
  gap: ${size.SIZE_014};
  justify-content: center;
  align-items: center;
`;
